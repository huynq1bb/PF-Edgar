# Model base images (for customize preview mock)

Add assets here for the Model Popup preview:

- `portrait-base.png` — Portrait mode base (face)
- `fullbody-base.png` — Full-body mode base

Paths used in app: `/examples/model/portrait-base.png`, `/examples/model/fullbody-base.png`
